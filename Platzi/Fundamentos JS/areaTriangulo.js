/*
let base= 5;
let altura= 7
function trianguloArea(base, altura){
  return base*altura/2
}
*/
let triangleArea = (base, altura)  => base*altura/2

// -

console.log(`Area de un triangulo de base ${base} y altura ${altura} es:
  ${triangleArea(base, altura)} `);
